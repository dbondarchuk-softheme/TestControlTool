@using BootstrapSupport
@using TestControlTool.Web.Models
@model Object
@{
    ViewBag.Title = "Task details";
}
<fieldset>
    <legend>@Model.GetLabel() <small>Details</small></legend>
    <dl> <!-- use this class on the dl if you want horizontal styling http://twitter.github.com/bootstrap/base-css.html#typography  class="dl-horizontal"-->     
        @foreach(var property in Model.VisibleProperties(MachineModel.NotShownProperties))
        {
            <dt>
                @property.GetLabel().ToSeparatedWords()
            </dt>
            <dd>
                @Html.Display(property.Name)                
            </dd>
        }
    </dl>
</fieldset>
<p>
    @Html.ActionLink("Edit", "EditTaskChilds", "Task", Model.GetIdValue(), null) |
    @Html.ActionLink("Back to List", "Tasks", "Home")
</p>
<script type="text/javascript">
    ShowTasks();
</script>